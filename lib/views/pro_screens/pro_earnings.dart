import 'package:flutter/material.dart';

class ProEarningsScreen extends StatelessWidget {
  const ProEarningsScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text("Pro Earnings"));
}
