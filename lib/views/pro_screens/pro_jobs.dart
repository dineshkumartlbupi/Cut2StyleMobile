import 'package:flutter/material.dart';

class ProJobsScreen extends StatelessWidget {
  const ProJobsScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text("Pro Jobs"));
}
