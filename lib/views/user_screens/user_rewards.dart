import 'package:flutter/material.dart';

class UserRewardsScreen extends StatelessWidget {
  const UserRewardsScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text("User Rewards"));
}
